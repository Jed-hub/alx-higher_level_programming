#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    for row in matrix:
        for i in range(1):
            i += 1
            for n in range(i):
                print("{}".format(matrix[n]))
